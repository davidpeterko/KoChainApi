{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Production: mainnet Bitcoin node.
  // NEVER hardcode credentials here. Supply them via environment variables:
  //   BitcoinRpcSettings__User=xxx
  //   BitcoinRpcSettings__Password=xxx
  //   BitcoinRpcSettings__Url=http://<your-node-ip>:8332/
  //
  // In Azure: use App Service Configuration or Key Vault.
  // In AWS: use Parameter Store or Secrets Manager.
  // In Docker: pass as environment variables in your compose file or container definition.
  "BitcoinRpcSettings": {
    "User": "",
    "Password": "",
    "Url": "http://127.0.0.1:8332/",
    "Network": "main"
  },
  "Blockstream": {
    "BaseUrl": "https://blockstream.info/api"
  }
}
