{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Staging: same network as development (testnet) but pointing to a dedicated staging node.
  // Credentials should be supplied via environment variables in your CI/CD pipeline,
  // not hardcoded here. Environment variable format: BitcoinRpcSettings__User=xxx
  "BitcoinRpcSettings": {
    "User": "",
    "Password": "",
    "Url": "http://127.0.0.1:18332/",
    "Network": "testnet"
  },
  "Blockstream": {
    "BaseUrl": "https://blockstream.info/testnet/api"
  }
}
